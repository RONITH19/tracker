# Google Sheets (published CSV) URL for dynamic product loading.
# Create a Google Sheet with these header columns (case-insensitive):
# id,name,description,priceStandard,pricePro,image,battery,waterproof,range
#
# Then publish/export it to a CSV link that is accessible without login.
VITE_PRODUCTS_SHEET_CSV_URL=""

